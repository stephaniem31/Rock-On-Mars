{% extends 'layout.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row text-center mt-3 mb-3">
            <h1>Bienvenue Loulou</h1>
        </div>
        <div class="row text-center mt-3 mb-3">
            <h4>Rechercher des activités en fonction du type</h4>
        </div>
        <div class="row text-center mt-3 mb-3">
            <form action="" method="GET" class="d-flex justify-content-center align-items-center">
                <select name="type" id="type" class="col form-control">
                    <option value="">--Choississez le type--</option>
                    <option value="sport">Sport</option>
                    <option value="divertissement">Divertissement</option>
                    <option value="culture">Culture</option>
                </select>
                <button type="submit" class="col-auto btn btn-primary">Rechercher</button>
            </form>
        </div>
        <div class="row text-center mt-3 mb-3">
            <h4>All Activities</h4>
        </div>
        <div class="row mt-3 mb-3 justify-content-center">
            {% for activity in activities %}
                {% include "Components/_card.html.twig" %}
            {% endfor %}
        </div>
    </div>
{% endblock %}